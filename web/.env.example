# قاعدة البيانات. SQLite للتشغيل المحلي، ويمكن التحويل إلى PostgreSQL بتغيير provider في prisma/schema.prisma
DATABASE_URL="file:./dev.db"

# مفتاح تشفير الأسرار (مفاتيح المزوّدين، أرقام الهواتف). إن تُرك فارغًا يُولَّد تلقائيًا في .data/app-secret
APP_SECRET=""

# العنوان العام للتطبيق (يُستخدم في رموز QR وروابط الـwebhooks). إن تُرك فارغًا يُستنتج من الطلب
APP_URL=""
